{
  "name": "@opensouls/monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "bump": "npm version patch --workspace packages && npm update @opensouls/engine @opensouls/soul soul-engine --workspace packages",
    "publish": "npm publish --workspace packages"
  },
  "workspaces": [
    "packages/*"
  ]
}
